import React from 'react';
import {SafeAreaView, ScrollView, StatusBar, Text} from 'react-native';

const Home = () => {
  return (
    <SafeAreaView>
      <StatusBar />
      <Text>Algo</Text>
      <ScrollView />
    </SafeAreaView>
  );
};

export default Home;
